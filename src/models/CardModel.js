export class CardModel {

    constructor(name, quantity, unit, icon, style) {
        this.name = name
        this.quantity = quantity;
        this.unit = unit;
        this.icon = icon;
        this.style = style;
    }
}